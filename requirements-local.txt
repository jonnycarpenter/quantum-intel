# Local-only dependencies (not in production Docker image)
# Install with: pip install -r requirements-local.txt

-r requirements.txt

# Storage — Local (ChromaDB + sentence-transformers pull in PyTorch)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Frontend
streamlit>=1.40.0
plotly>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
